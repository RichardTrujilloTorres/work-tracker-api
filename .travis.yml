language: php

php: 7.1

before_script:
  - cp .env.travis .env
  - mysql -e 'create database if not exists work_tracker;'
  - travis_retry composer self-update
  - travis_retry composer install --prefer-source --no-interaction

script: vendor/bin/phpunit
